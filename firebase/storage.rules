rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if request.auth != null;
      // Restrict writes in app code using path-based rules later if needed
      allow write: if false;
    }
  }
}

